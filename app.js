const SUPABASE_URL='https://uvujytjdafcyacawcirp.supabase.co';
const SUPABASE_KEY='sb_publishable__mofqtls_ru76dDkIJbIew_G2f_wUKE';
const LOCAL_KEY='meu_treino_reset_v2';
const CLOUD_PREFIX='resetv1|';

const workouts={
 Segunda:{icon:'🟢',title:'Pernas + Peito',ex:[['Abdominal máquina',3,'12–15'],['Pêndulo',4,'6–10'],['Cadeira extensora',3,'10–15'],['Stiff com barra ou halteres',3,'8–10'],['Supino inclinado com halteres',4,'8–10'],['Supino reto máquina',3,'8–12'],['Crucifixo máquina',2,'12–15']]},
 Terça:{icon:'🔵',title:'Costas + Bíceps',ex:[['Barra fixa ou puxada neutra',4,'6–10'],['Remada articulada com apoio no peito',4,'8–12'],['Remada unilateral na polia',3,'10–12'],['PULLDOWN',3,'10–15'],['Rosca direta com barra W',3,'8–12'],['Rosca inclinada com halteres',3,'10–12'],['Rosca martelo com halteres',2,'10–12']]},
 Quarta:{icon:'🟠',title:'Pernas + Ombros',ex:[['Abdominal no Cross',3,'12–15'],['Leg press 45°',4,'8–12'],['Cadeira flexora bilateral',4,'8–12'],['Mesa flexora',3,'10–15'],['Desenvolvimento máquina',3,'8–10'],['Elevação lateral na polia',4,'10–15'],['Crucifixo inverso máquina',3,'12–15'],['Panturrilha em pé ou no leg press',4,'10–15']]},
 Quinta:{icon:'🔴',title:'Peito + Tríceps',ex:[['Supino reto com barra',4,'6–10'],['Supino inclinado máquina',3,'8–12'],['Supino reto máquina',3,'8–12'],['Crossover de baixo para cima',2,'12–15'],['Tríceps francês unilateral na polia',3,'10–12'],['Tríceps barra V',3,'8–12'],['Tríceps testa com barra W',2,'10–12']]},
 Sexta:{icon:'🟣',title:'Full Body + Pontos fracos',ex:[['Abdominal máquina',3,'12–15'],['Agachamento Hack',3,'8–12'],['Flexora unilateral',3,'10–12'],['Remada baixa triângulo',3,'8–12'],['Puxada alta pronada',3,'8–12'],['Elevação lateral',3,'12–15'],['Rosca Scott máquina',2,'10–12'],['Tríceps corda',2,'10–15']]},
 Sábado:{icon:'🟣',title:'Opcional • Pontos fracos',ex:[['Elevação lateral',3,'12–15'],['Crucifixo inverso',3,'12–15'],['Rosca martelo',3,'10–12'],['Tríceps corda',3,'10–12'],['Panturrilha sentado',4,'12–15']]},
 Domingo:{icon:'⚪',title:'Recuperação / Cardio',ex:[]}
};
const cardioTypes=['Bicicleta','Esteira / caminhada','Corrida','Elíptico','Outro'];
const restPreset={'Abdominal máquina':60,'Abdominal no Cross':60,'Pêndulo':120,'Cadeira extensora':75,'Stiff com barra ou halteres':120,'Supino inclinado com halteres':120,'Supino reto máquina':90,'Crucifixo máquina':75,'Barra fixa ou puxada neutra':120,'Remada articulada com apoio no peito':120,'Remada unilateral na polia':90,'PULLDOWN':90,'Rosca direta com barra W':75,'Rosca inclinada com halteres':75,'Rosca martelo com halteres':75,'Leg press 45°':120,'Cadeira flexora bilateral':90,'Mesa flexora':90,'Desenvolvimento máquina':90,'Elevação lateral na polia':60,'Crucifixo inverso máquina':60,'Panturrilha em pé ou no leg press':75,'Supino reto com barra':150,'Supino inclinado máquina':120,'Crossover de baixo para cima':75,'Tríceps francês unilateral na polia':75,'Tríceps barra V':75,'Tríceps testa com barra W':75,'Hack squat':120,'Agachamento Hack':120,'Flexora unilateral':90,'Remada baixa triângulo':120,'Puxada alta pronada':90,'Elevação lateral':60,'Rosca Scott máquina':75,'Tríceps corda':75,'Panturrilha':75,'Panturrilha sentado':75,'Crucifixo inverso':60,'Rosca martelo':75};

let data=JSON.parse(localStorage.getItem(LOCAL_KEY)||'null')||{logs:{}};
let drafts={},current='Segunda',editDate=null,user=null,sb=null,cloudReady=false,saveTimer=null,authBusy=false,restIntervals={},restDebounce={},generalTimer=null;
const $=id=>document.getElementById(id);
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const today=()=>{const d=new Date();return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')};
const key=(day,date)=>day+'|'+date;
const dateBR=d=>new Date(d+'T12:00:00').toLocaleDateString('pt-BR',{weekday:'long',day:'2-digit',month:'2-digit',year:'numeric'});
const days=['Segunda','Terça','Quarta','Quinta','Sexta','Sábado','Domingo'];
function activeDate(){return editDate||today()}
function setStatus(text,on=false){const e=$('cloudStatus');if(e){e.textContent=text;e.className='status '+(on?'on':'')}}
function persist(){try{localStorage.setItem(LOCAL_KEY,JSON.stringify(data))}catch(e){}}
function localSave(){persist();setStatus(user&&cloudReady?'☁ Online • salvo':'☁ Offline/local',!!(user&&cloudReady))}
function blankLog(day,date){return{day,date,rows:{},notes:'',duration:'',avgBpm:'',calories:'',effort:'',cardio:{type:'',duration:'',distance:'',calories:'',avgBpm:''},completed:true}}
function clone(v){return JSON.parse(JSON.stringify(v))}
function ensureDraft(day=current,date=activeDate()){const k=key(day,date);if(!drafts[k])drafts[k]=data.logs[k]?clone(data.logs[k]):blankLog(day,date);return drafts[k]}
function activeLog(day=current,date=activeDate()){return drafts[key(day,date)]||data.logs[key(day,date)]||null}
function exFor(day){return workouts[day]?.ex||[]}
function initClient(){try{sb=window.supabase?.createClient(SUPABASE_URL,SUPABASE_KEY);return !!sb}catch(e){console.error(e);return false}}
function loadSupabaseScript(){return new Promise(resolve=>{
  if(window.supabase?.createClient){resolve(true);return}
  const script=document.createElement('script');
  script.src='https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.110.0/dist/umd/supabase.js';
  script.crossOrigin='anonymous';
  let done=false;
  const finish=ok=>{if(done)return;done=true;clearTimeout(timer);script.onload=null;script.onerror=null;resolve(ok)};
  const timer=setTimeout(()=>finish(false),1800);
  script.onload=()=>finish(!!window.supabase?.createClient);
  script.onerror=()=>finish(false);
  document.head.appendChild(script);
})}
function enterOfflineMode(reason='☁ Offline • salvo neste aparelho'){
  user={id:'offline-local',offline:true};cloudReady=false;setStatus(reason);render();
}
async function init(){
  try{localStorage.removeItem('meu_treino_reset_v1');['meu_treino_spotify_client_id','meu_treino_spotify_token','spotify_verifier','spotify_state'].forEach(k=>localStorage.removeItem(k))}catch(e){}
  cleanLocalData();
  // Nunca deixe o carregamento do Supabase bloquear o aplicativo. O treino abre localmente mesmo sem internet.
  if(!navigator.onLine){enterOfflineMode();return}
  setStatus('☁ Preparando...',true);
  const loaded=await loadSupabaseScript();
  if(!loaded||!initClient()){enterOfflineMode();return}
  setStatus('☁ Verificando...',true);
  let sessionResult=null;
  try{
    sessionResult=await Promise.race([
      sb.auth.getSession(),
      new Promise((_,reject)=>setTimeout(()=>reject(new Error('Supabase session timeout')),2200))
    ]);
    user=sessionResult?.data?.session?.user||null;
  }catch(e){console.warn('Sessão Supabase não respondeu a tempo:',e);enterOfflineMode();return}
  render();
  if(user) await loadCloud(); else setStatus('☁ Pronto para entrar');
}
async function signIn(){if(authBusy)return;const email=$('email')?.value.trim(),password=$('password')?.value||'';if(!email||!password){alert('Preencha e-mail e senha.');return}if(!sb){alert('A conexão ainda carregou. Recarregue a página.');return}authBusy=true;const btn=$('loginBtn');if(btn){btn.disabled=true;btn.textContent='ENTRANDO...'}setStatus('☁ Conectando...',true);try{const {data:r,error}=await sb.auth.signInWithPassword({email,password});if(error)throw error;user=r.user;cloudReady=false;render();await loadCloud()}catch(e){console.error(e);setStatus('☁ Pronto para entrar');alert(e?.message||'Não foi possível entrar.')}finally{authBusy=false;if(btn){btn.disabled=false;btn.textContent='ENTRAR'}}}
async function signUp(){const email=$('email')?.value.trim(),password=$('password')?.value||'';if(!email||password.length<6){alert('Informe um e-mail e uma senha com pelo menos 6 caracteres.');return}if(!sb){alert('A conexão ainda não carregou.');return}const {error}=await sb.auth.signUp({email,password});alert(error?error.message:'Conta criada. Confirme o e-mail se solicitado e faça login.')}
async function signOut(){try{await sb?.auth.signOut()}catch(e){}user=null;cloudReady=false;drafts={};editDate=null;current='Segunda';render();setStatus('☁ Pronto para entrar')}
function validSavedLog(l){return !!(l?.date&&l?.completed!==false&&Object.values(l.rows||{}).some(r=>Object.keys(r||{}).some(k=>/^kg\d+$/.test(k)&&Number(r[k])>0)&&Object.keys(r||{}).some(k=>/^reps\d+$/.test(k)&&Number(r[k])>0)))}
function cleanLocalData(){const flag='meu_treino_legacy_cleanup_v4';try{if(localStorage.getItem(flag)==='1')return}catch(e){}let changed=false;Object.keys(data.logs||{}).forEach(k=>{const l=data.logs[k];if(!validSavedLog(l)){delete data.logs[k];changed=true}});if(changed)persist();try{localStorage.setItem(flag,'1')}catch(e){}}
async function loadCloud(){if(!user||user.offline||!sb)return;try{const {data:rows,error}=await sb.from('workout_logs').select('log_key,payload').like('log_key',CLOUD_PREFIX+'%').order('workout_date',{ascending:false});if(error)throw error;cloudReady=true;for(const r of rows||[]){const k=r.log_key.replace(CLOUD_PREFIX,'');if(validSavedLog(r.payload))data.logs[k]=r.payload}cleanLocalData();persist();render();setStatus('☁ Online • salvo',true)}catch(e){console.error('cloud load',e);cloudReady=false;setStatus('⚠ Nuvem indisponível • local salvo')}}
function queueSave(k){clearTimeout(saveTimer);saveTimer=setTimeout(()=>cloudSave(k),250)}
async function cloudSave(k){if(!user||user.offline||!sb||!data.logs[k])return;try{const l=data.logs[k];const {error}=await sb.from('workout_logs').upsert({user_id:user.id,log_key:CLOUD_PREFIX+k,day:l.day,workout_date:l.date,payload:l,updated_at:new Date().toISOString()},{onConflict:'user_id,log_key'});if(error)throw error;cloudReady=true;setStatus('☁ Online • salvo',true)}catch(e){console.error('cloud save',e);cloudReady=false;setStatus('⚠ Salvo neste aparelho')}}
function commitSaved(k){const d=drafts[k];if(!d)return false;if(!validDraft(d)){alert('Preencha pelo menos uma série com carga e repetições antes de salvar.');return false}stopGeneralTimer(false);d.completed=true;d.timerStartedAt=null;d.timerElapsed=elapsedFrom(d);d.duration=d.timerElapsed?fmtDuration(d.timerElapsed):d.duration||'';d.tonnageKg=volumeForLog(d);d.tonnes=d.tonnageKg/1000;data.logs[k]=clone(d);delete drafts[k];editDate=null;persist();queueSave(k);render();return true}
function validDraft(l){return !!Object.values(l?.rows||{}).some(r=>{for(let s=0;s<20;s++)if(Number(r?.['kg'+s])>0&&Number(r?.['reps'+s])>0)return true;return false})}
function autoStartRep(k){const l=drafts[k]||ensureDraft(current,activeDate());startGeneralTimer(k,l)}
function setVal(k,i,s,f,v){const l=drafts[k]||ensureDraft(current,activeDate());l.rows[i]??={};l.rows[i][f+s]=v;localSave();if(f==='reps'&&String(v??'').trim()&&Number(v)>0)startGeneralTimer(k,l);updateWorkoutSummary();refreshExerciseCoach(i)}
function setObs(k,i,v){const l=drafts[k]||ensureDraft(current,activeDate());l.rows[i]??={};l.rows[i].observation=v}
function setMetric(k,f,v){const l=drafts[k]||ensureDraft(current,activeDate());l[f]=v;updateWorkoutSummary()}
function setCardio(k,f,v){const l=drafts[k]||ensureDraft(current,activeDate());l.cardio??={};l.cardio[f]=v}
function setNotes(k,v){const l=drafts[k]||ensureDraft(current,activeDate());l.notes=v}
async function deleteLog(k){if(!data.logs[k]){delete drafts[k];render();return}if(!confirm('Excluir este registro de treino? Esta ação não pode ser desfeita.'))return;delete data.logs[k];delete drafts[k];persist();if(user&&!user.offline&&sb){try{await sb.from('workout_logs').delete().eq('user_id',user.id).eq('log_key',CLOUD_PREFIX+k)}catch(e){console.error(e)}}render()}
function clearDay(){const k=key(current,activeDate());if(drafts[k]){delete drafts[k];stopGeneralTimer();render();return}if(data.logs[k])deleteLog(k);else render()}
function editLog(k){if(!data.logs[k])return;drafts[k]=clone(data.logs[k]);current=data.logs[k].day;editDate=data.logs[k].date;render()}
function dayNav(){return '<div class="daystrip">'+days.map(d=>'<button class="'+(current===d?'active':'')+'" onclick="go(\''+d+'\')">'+workouts[d].icon+'<small>'+d.slice(0,3).toUpperCase()+'</small></button>').join('')+'</div>'}
function topNav(){return '<div class="nav"><button class="'+(days.includes(current)?'active':'')+'" onclick="go(\'Segunda\')">🏋️ Treino</button><button class="'+(current==='Histórico'?'active':'')+'" onclick="go(\'Histórico\')">📈 Histórico</button><button class="'+(current==='Relatórios'?'active':'')+'" onclick="go(\'Relatórios\')">📊 Relatórios</button><button class="'+(current==='Dados'?'active':'')+'" onclick="go(\'Dados\')">⚙️ Dados</button></div>'}
function bottomNav(){return '<div class="bottomnav"><button class="'+(days.includes(current)?'active':'')+'" onclick="go(\'Segunda\')">🏋️<span>Treino</span></button><button class="'+(current==='Histórico'?'active':'')+'" onclick="go(\'Histórico\')">📈<span>Histórico</span></button><button class="'+(current==='Relatórios'?'active':'')+'" onclick="go(\'Relatórios\')">📊<span>Relatórios</span></button><button class="'+(current==='Dados'?'active':'')+'" onclick="go(\'Dados\')">⚙️<span>Dados</span></button></div>'}
function go(x){if(generalTimer&&days.includes(current)&&x!==current)stopGeneralTimer();if(!days.includes(x))editDate=null;else if(x!==current)editDate=null;current=x;render()}
function fieldInput(el){el.classList.toggle('filled',el.value!=='')}
function advanceField(el){if(!el.value)return;const row=el.closest('.setrow');if(!row)return;if(el.dataset.kind==='kg')row.querySelector('input[data-kind="reps"]')?.focus();else{const rows=[...document.querySelectorAll('.setrow')],ri=rows.indexOf(row);rows[ri+1]?.querySelector('input[data-kind="kg"]')?.focus()}}
function enterHandler(e){if(e.key!=='Enter'||!e.target.matches('input.field'))return;e.preventDefault();if(e.target.dataset.kind==='kg')e.target.closest('.setrow')?.querySelector('input[data-kind="reps"]')?.focus();else{const rows=[...document.querySelectorAll('.setrow')],row=e.target.closest('.setrow'),ri=rows.indexOf(row);rows[ri+1]?.querySelector('input[data-kind="kg"]')?.focus()}}
function volumeForLog(l){let total=0;Object.values(l?.rows||{}).forEach(r=>{for(let s=0;s<20;s++){const kg=Number(r?.['kg'+s]),reps=Number(r?.['reps'+s]);if(kg>0&&reps>0)total+=kg*reps}});return total}
function fmtDuration(s){s=Math.max(0,Math.floor(s||0));return Math.floor(s/3600)+':'+String(Math.floor((s%3600)/60)).padStart(2,'0')+':'+String(s%60).padStart(2,'0')}
function elapsedFrom(l){return l?.timerStartedAt?Math.max(0,Math.floor((Date.now()-new Date(l.timerStartedAt).getTime())/1000)+Number(l.timerElapsed||0)):Number(l?.timerElapsed||0)}
function startGeneralTimer(k,l=drafts[k]||ensureDraft(current,activeDate())){if(l.timerStartedAt)return;l.timerStartedAt=new Date().toISOString();l.timerElapsed=Number(l.timerElapsed||0);if(generalTimer)clearInterval(generalTimer);generalTimer=setInterval(()=>{const d=drafts[k];if(!d?.timerStartedAt){clearInterval(generalTimer);generalTimer=null;return}const out=$('generalTimerClock');if(out)out.textContent=fmtDuration(elapsedFrom(d));},500);updateTimerDisplay(k)}
function stopGeneralTimer(saveElapsed=true){const k=key(current,activeDate()),l=drafts[k];if(l?.timerStartedAt){l.timerElapsed=elapsedFrom(l);l.timerStartedAt=null}if(generalTimer){clearInterval(generalTimer);generalTimer=null}if(saveElapsed&&l)l.duration=fmtDuration(l.timerElapsed);updateTimerDisplay(k)}
function updateTimerDisplay(k){const l=drafts[k]||data.logs[k],out=$('generalTimerClock');if(out)out.textContent=fmtDuration(elapsedFrom(l))}
function timerCard(l,k){const running=!!l.timerStartedAt;return '<section class="generalTimerCard"><div class="gtHeader"><div><span class="gtKicker">CRONÔMETRO GERAL</span><strong class="gtStatus">'+(running?'EM ANDAMENTO':'PRONTO')+'</strong></div><span class="gtDot '+(running?'running':'')+'"></span></div><div id="generalTimerClock" class="gtClock">'+fmtDuration(elapsedFrom(l))+'</div><div class="gtActions"><button class="primary" onclick="startGeneralTimer(\''+k+'\')">INICIAR</button><button class="secondary" onclick="stopGeneralTimer()">PARAR</button><button class="secondary" onclick="resetGeneralTimer()">ZERAR</button></div></section>'}
function resetGeneralTimer(){const k=key(current,activeDate()),l=drafts[k];if(l){l.timerStartedAt=null;l.timerElapsed=0;l.duration=''}if(generalTimer){clearInterval(generalTimer);generalTimer=null}render()}
function restButton(name,i){const sec=restPreset[name]||90;return '<div class="restbox" id="rest-'+i+'"><span>DESCANSO</span><b>'+fmtTime(sec)+'</b><button type="button" onclick="toggleRest('+i+','+sec+')">▶</button></div>'}
function fmtTime(s){return Math.floor(s/60)+':'+String(Math.max(0,s%60)).padStart(2,'0')}
function showRestFinishedNotice(){
  let n=$('restFinishedNotice');
  if(!n){
    n=document.createElement('div');
    n.id='restFinishedNotice';
    n.className='restFinishedNotice';
    n.setAttribute('role','status');
    n.setAttribute('aria-live','polite');
    document.body.appendChild(n);
  }
  n.textContent='Descanso finalizado';
  n.classList.remove('show');
  void n.offsetWidth;
  n.classList.add('show');
  clearTimeout(window.__restFinishedNoticeTimer);
  window.__restFinishedNoticeTimer=setTimeout(()=>n.classList.remove('show'),2200);
}
function finishRest(i){const box=$('rest-'+i);if(!box)return;box.classList.add('done');const b=box.querySelector('b'),btn=box.querySelector('button');b.textContent='0:00';btn.textContent='↻';showRestFinishedNotice();if(navigator.vibrate)try{navigator.vibrate([120,60,120])}catch(e){}}
function startRest(i,seconds,automatic=false){const box=$('rest-'+i);if(!box)return;if(restIntervals[i])return;if(restDebounce[i]){clearTimeout(restDebounce[i]);delete restDebounce[i]}box.classList.remove('done');box.querySelector('b').textContent=fmtTime(seconds);box.querySelector('button').textContent='⏸';const end=Date.now()+seconds*1000;restIntervals[i]=setInterval(()=>{const left=Math.max(0,Math.ceil((end-Date.now())/1000));box.querySelector('b').textContent=fmtTime(left);if(left<=0){clearInterval(restIntervals[i]);delete restIntervals[i];finishRest(i)}},200);if(automatic&&navigator.vibrate)try{navigator.vibrate(60)}catch(e){}}
function toggleRest(i,seconds){if(restDebounce[i]){clearTimeout(restDebounce[i]);delete restDebounce[i]}if(restIntervals[i]){clearInterval(restIntervals[i]);delete restIntervals[i];const box=$('rest-'+i);if(box){box.classList.remove('done');box.querySelector('b').textContent=fmtTime(seconds);box.querySelector('button').textContent='▶'}return}startRest(i,seconds,false)}
function queueRestFromInput(k,i,s,value,seconds){
  const text=String(value??'').trim();
  const reps=Number(value);
  if(!text||!Number.isFinite(reps)||reps<=0)return;
  const l=drafts[k]||ensureDraft(current,activeDate());
  l.rows[i]??={};
  l.rows[i]['reps'+s]=value;
  localSave();
  clearTimeout(restDebounce[i]);
  restDebounce[i]=setTimeout(()=>{
    delete restDebounce[i];
    // O descanso deve iniciar ao terminar QUALQUER série. Não exigimos carga
    // nem dependemos de seletores frágeis do DOM: a repetição já foi salva acima.
    if(restIntervals[i]){clearInterval(restIntervals[i]);delete restIntervals[i]}
    startGeneralTimer(k,l);
    startRest(i,seconds,true);
    updateWorkoutSummary();
  },350);
}
function previousExercise(name,date){let best=null;Object.values(data.logs||{}).forEach(l=>{if(!validSavedLog(l)||String(l.date)>=String(date))return;const exs=workouts[l.day]?.ex||[];Object.keys(l.rows||{}).forEach(i=>{if(exs[Number(i)]?.[0]!==name)return;const row=l.rows[i];if(!best||String(l.date)>best.date)best={date:String(l.date),row}})});return best}
function coachDecision(ex,row,oldInfo){const m=String(ex[2]).match(/(\d+)\s*[–-]\s*(\d+)/);if(!m)return['MANTER CARGA','Faixa de repetições: '+ex[2]+'.'];const lo=+m[1],hi=+m[2],cur=setsFrom(row,ex[1]),old=oldInfo?.row||null,prev=setsFrom(old,ex[1]),currentComplete=cur.length===ex[1];if(!cur.length&&!prev.length)return['PRIMEIRO REGISTRO','Ainda não existe treino salvo para comparar. Registre carga e reps e o Coach passará a acompanhar este exercício.'];const base=currentComplete?cur:(prev.length?prev:cur),avg=base.reduce((a,x)=>a+x.reps,0)/base.length,vol=base.reduce((a,x)=>a+x.kg*x.reps,0),bestSet=base.reduce((a,x)=>!a||x.kg>a.kg||(x.kg===a.kg&&x.reps>a.reps)?x:a,null);const source=currentComplete?'sessão atual':'último treino salvo';let action='MANTER CARGA',reason=`${source[0].toUpperCase()+source.slice(1)}: ${base.map(x=>x.kg+' kg × '+x.reps+' reps').join(' • ')}. Média ${avg.toFixed(1)} reps, volume ${Math.round(vol)} kg. Meta ${ex[2]}.`;if(currentComplete&&cur.every(x=>x.reps>=hi)){action='AUMENTAR CARGA';reason+=` Você bateu o topo (${hi}) em todas as séries. Próxima sessão: suba cerca de 2–5% e volte para a parte baixa da faixa.`}else if(currentComplete&&cur.some(x=>x.reps<lo)){action='CONSOLIDAR';reason+=` Há série abaixo do mínimo (${lo}). Mantenha a carga até todas as séries entrarem na faixa.`}else if(currentComplete&&cur.every(x=>x.reps>=lo)){action='AUMENTAR REPS';reason+=` Todas as séries estão na faixa. Mantenha a carga e tente ganhar 1 rep nas séries antes de subir o peso.`}else if(prev.length&&currentComplete){const pv=prev.reduce((a,x)=>a+x.kg*x.reps,0);const prevMax=Math.max(...prev.map(x=>x.kg));if(bestSet.kg>prevMax){action='CONSOLIDAR';reason+=` A carga máxima subiu de ${prevMax} para ${bestSet.kg} kg. Consolide essa carga antes de novo aumento.`}else if(vol>pv){action='EVOLUÇÃO';reason+=` O volume subiu ${Math.round(vol-pv)} kg em relação ao treino anterior (${Math.round(pv)} → ${Math.round(vol)} kg).` }else if(vol<pv){action='ATENÇÃO';reason+=` O volume caiu ${Math.round(pv-vol)} kg em relação ao anterior (${Math.round(pv)} → ${Math.round(vol)} kg). Priorize execução e recuperação.`}else{reason+=` O desempenho ficou estável; tente acrescentar 1–2 reps mantendo a técnica.`}}else{reason+=` Esta sessão ainda está incompleta; a recomendação acima usa o último treino salvo como referência.`}if(oldInfo?.date)reason+=` Última sessão salva: ${dateBR(oldInfo.date)}.`;return[action,reason]}
function setsFrom(row,count=20){const out=[];for(let i=0;i<count;i++){const kg=Number(row?.['kg'+i]),reps=Number(row?.['reps'+i]);if(kg>0&&reps>0)out.push({kg,reps,i})}return out}
function exerciseCard(ex,i,k){const l=activeLog(current,activeDate())||blankLog(current,activeDate()),r=l.rows?.[i]||{},oldInfo=previousExercise(ex[0],activeDate()),old=oldInfo?.row,[action,reason]=coachDecision(ex,r,oldInfo);let sets='';for(let s=0;s<ex[1];s++){const kg=r['kg'+s],reps=r['reps'+s],ghostKg=old?.['kg'+s],ghostReps=old?.['reps'+s];const restHook=`;queueRestFromInput('${k}',${i},${s},this.value,${restPreset[ex[0]]||90})`;sets+='<div class="setrow"><div class="setno">'+(s+1)+'</div><input class="field" data-kind="kg" type="number" step="0.5" inputmode="decimal" placeholder="'+(ghostKg!==undefined?esc(ghostKg+' kg'):'kg')+'" value="'+esc(kg??'')+'" oninput="fieldInput(this);setVal(\''+k+'\','+i+','+s+',\'kg\',this.value)" onchange="setVal(\''+k+'\','+i+','+s+',\'kg\',this.value)"><input class="field" data-kind="reps" type="number" inputmode="numeric" placeholder="'+(ghostReps!==undefined?esc(ghostReps+' reps'):'reps')+'" value="'+esc(reps??'')+'" oninput="fieldInput(this);setVal(\''+k+'\','+i+','+s+',\'reps\',this.value)'+restHook+'" onchange="setVal(\''+k+'\','+i+','+s+',\'reps\',this.value)"></div>'}
const guide=typeof window.exerciseGuideMarkup==='function'?window.exerciseGuideMarkup(ex[0]):'';return '<article class="exercise"><div class="exhead"><div class="ex-title-wrap">'+guide+'<div><div class="exname">'+(i+1)+'. '+esc(ex[0])+'</div><div class="meta">'+ex[1]+' séries • '+ex[2]+' reps</div>'+restButton(ex[0],i)+'</div></div><span class="tag">'+ex[2]+'</span></div><div class="sets"><div class="sethead"><span>SÉRIE</span><span>CARGA KG</span><span>REPS</span></div>'+sets+'</div><div class="coachAiBox"><div class="coachIcon">🤖</div><div><div class="coachTitle">COACH IA • '+esc(action)+'</div><div class="coachAction">'+esc(ex[0])+'</div><span class="coachReason">'+esc(reason)+'</span></div></div><textarea class="obs" rows="2" placeholder="Observação: técnica, falha, dor, execução..." onchange="setObs(\''+k+'\','+i+',this.value)">'+esc(r.observation||'')+'</textarea></article>'}
function cardioCard(l,k){const c=l.cardio||{};return '<section class="card cardioCard"><div class="cardTitleRow"><div><h2>❤️ Cardio</h2><p class="muted">Você pode registrar cardio em qualquer dia.</p></div><span class="cardioIcon">🏃</span></div><div class="metrics cardioMetrics"><label>TIPO<select onchange="setCardio(\''+k+'\',\'type\',this.value)"><option value="">Escolha</option>'+cardioTypes.map(x=>'<option value="'+esc(x)+'" '+(c.type===x?'selected':'')+'>'+esc(x)+'</option>').join('')+'</select></label><label>TEMPO<input placeholder="30 min" value="'+esc(c.duration||'')+'" onchange="setCardio(\''+k+'\',\'duration\',this.value)"></label><label>DISTÂNCIA<input placeholder="5 km" value="'+esc(c.distance||'')+'" onchange="setCardio(\''+k+'\',\'distance\',this.value)"></label><label>CALORIAS<input type="number" placeholder="300" value="'+esc(c.calories||'')+'" onchange="setCardio(\''+k+'\',\'calories\',this.value)"></label><label>BPM MÉDIO<input type="number" placeholder="120" value="'+esc(c.avgBpm||'')+'" onchange="setCardio(\''+k+'\',\'avgBpm\',this.value)"></label></div></section>'}
function refreshExerciseCoach(i){const ex=workouts[current]?.ex?.[i];const card=document.querySelectorAll('.exercise')[i];if(!ex||!card)return;const l=activeLog(current,activeDate())||blankLog(current,activeDate()),r=l.rows?.[i]||{},oldInfo=previousExercise(ex[0],activeDate()),[action,reason]=coachDecision(ex,r,oldInfo);const box=card.querySelector('.coachAiBox');if(box)box.innerHTML='<div class="coachIcon">🤖</div><div><div class="coachTitle">COACH IA • '+esc(action)+'</div><div class="coachAction">'+esc(ex[0])+'</div><span class="coachReason">'+esc(reason)+'</span></div>'}
function workout(){const ex=exFor(current),d=activeDate(),k=key(current,d),l=activeLog(current,d)||blankLog(current,d),vol=volumeForLog(l);return topNav()+'<section class="hero"><div class="eyebrow">Treino de '+(editDate?'edição':'hoje')+'</div><h1>'+workouts[current].icon+' '+workouts[current].title+'</h1><p>'+dateBR(d)+' • Registre carga, repetições e métricas.</p></section>'+dayNav()+'<div class="clear-workout-bar"><button type="button" class="clear-workout-btn" onclick="clearDay()">🧹 Limpar treino de '+esc(current)+'</button></div>'+timerCard(l,k)+'<div class="stats"><div class="stat"><span>Exercícios</span><b>'+ex.length+'</b></div><div class="stat"><span>Séries</span><b>'+ex.reduce((a,e)=>a+e[1],0)+'</b></div><div class="stat"><span>Tempo</span><b id="summaryDuration">'+esc(l.duration||'—')+'</b></div><div class="stat"><span>Calorias</span><b>'+esc(l.calories||'—')+'</b></div><div class="stat"><span>Peso levantado</span><b>'+Math.round(vol).toLocaleString('pt-BR')+' kg</b></div></div>'+(ex.length?'<div class="exercise-grid">'+ex.map((e,i)=>exerciseCard(e,i,k)).join('')+'</div>':'')+cardioCard(l,k)+'<section class="card metricsCard"><h2>📊 Métricas do treino</h2><div class="metrics"><label>⏱️ TEMPO<input placeholder="Automático" value="'+esc(l.duration||'')+'" readonly></label><label>❤️ BPM MÉDIO<input type="number" placeholder="119" value="'+esc(l.avgBpm||'')+'" onchange="setMetric(\''+k+'\',\'avgBpm\',this.value)"></label><label>🔥 CALORIAS<input type="number" placeholder="500" value="'+esc(l.calories||'')+'" onchange="setMetric(\''+k+'\',\'calories\',this.value)"></label><label>💪 ESFORÇO<select onchange="setMetric(\''+k+'\',\'effort\',this.value)"><option value="">RPE</option>'+Array.from({length:10},(_,i)=>'<option value="'+(i+1)+'" '+(String(l.effort)===String(i+1)?'selected':'')+'>'+(i+1)+'/10</option>').join('')+'</select></label></div><label class="noteslabel">📝 Observações gerais<textarea class="notes" rows="3" placeholder="Como foi o treino? Energia, desempenho, dores..." onchange="setNotes(\''+k+'\',this.value)">'+esc(l.notes||'')+'</textarea></label><div class="actions"><button class="primary" onclick="commitSaved(\''+k+'\')">✓ Salvar treino</button><button class="danger" onclick="clearDay()">🧹 Limpar treino</button></div></section>'}
function updateWorkoutSummary(){const l=activeLog(current,activeDate());const out=$('summaryDuration');if(out)out.textContent=l?.duration||'—';updateTimerDisplay(key(current,activeDate()))}
function loadFilled(){document.querySelectorAll('input.field').forEach(fieldInput)}
function history(){const logs=Object.values(data.logs||{}).filter(validSavedLog).sort((a,b)=>String(b.date).localeCompare(String(a.date)));if(!logs.length)return topNav()+'<section class="card empty"><div class="emptyicon">📈</div><h2>Nenhum treino registrado</h2><p>Depois que você salvar seu primeiro treino, ele aparecerá aqui.</p></section>'+bottomNav();const rows=logs.map(l=>{const k=key(l.day,l.date),filled=Object.values(l.rows||{}).filter(r=>setsFrom(r).length).length,c=l.cardio||{},vol=volumeForLog(l);return '<article class="historyrow"><div class="historyhead"><div><b>'+esc(l.day)+' — '+dateBR(l.date)+'</b><small>'+esc(workouts[l.day]?.title||'Treino opcional')+'</small></div><div class="historyBtns"><button class="secondary smallbtn" onclick="editLog(\''+k+'\')">✏️ Editar</button><button class="danger smallbtn" onclick="deleteLog(\''+k+'\')">Excluir</button></div></div><div class="chips">'+(filled?'<span>🏋️ '+filled+' exercícios</span>':'')+'<span>🏋️ '+Math.round(vol).toLocaleString('pt-BR')+' kg</span>'+(l.calories?'<span>🔥 '+esc(l.calories)+' kcal</span>':'')+(l.avgBpm?'<span>❤️ '+esc(l.avgBpm)+' bpm</span>':'')+(l.duration?'<span>⏱️ '+esc(l.duration)+'</span>':'')+(l.effort?'<span>💪 RPE '+esc(l.effort)+'</span>':'')+(c.type?'<span>🏃 '+esc(c.type)+(c.duration?' • '+esc(c.duration):'')+'</span>':'')+'</div></article>'}).join('');return topNav()+'<section class="hero compact"><div class="eyebrow">Histórico</div><h1>Seus registros</h1><p>Somente treinos salvos pelo botão “Salvar treino”.</p></section><section class="card">'+rows+'</section>'+bottomNav()}
function reportRange(mode){const all=Object.values(data.logs||{}).filter(validSavedLog).sort((a,b)=>String(a.date).localeCompare(String(b.date))),now=new Date(),iso=today();if(mode==='all')return all;if(mode==='day')return all.filter(l=>l.date===iso);if(mode==='week'){const start=new Date(now.getFullYear(),now.getMonth(),now.getDate());start.setDate(start.getDate()-((start.getDay()+6)%7));const end=new Date(start);end.setDate(start.getDate()+6);return all.filter(l=>{const d=new Date(l.date+'T12:00:00');return d>=start&&d<=end})}const prefix=iso.slice(0,7);return all.filter(l=>l.date.startsWith(prefix))}
function reportStats(ls){const volume=ls.reduce((a,l)=>a+volumeForLog(l),0),tons=volume/1000,sets=ls.reduce((a,l)=>a+Object.values(l.rows||{}).reduce((n,r)=>n+setsFrom(r).length,0),0),bpms=ls.map(l=>Number(l.avgBpm)).filter(Boolean),rpes=ls.map(l=>Number(l.effort)).filter(Boolean);return{workouts:ls.length,volume,tons,sets,avgBpm:bpms.length?bpms.reduce((a,b)=>a+b,0)/bpms.length:null,avgRpe:rpes.length?rpes.reduce((a,b)=>a+b,0)/rpes.length:null}}
function exerciseNames(){const names=new Set();Object.values(workouts).forEach(w=>(w.ex||[]).forEach(e=>names.add(e[0])));return[...names].sort((a,b)=>a.localeCompare(b,'pt-BR'))}
function coachPeriod(mode){const cur=reportRange(mode);if(!cur.length)return{score:'—',title:mode==='day'?'Análise do treino do dia':mode==='week'?'Análise semanal':'Análise mensal',text:'Nenhum treino salvo neste período.',next:'Salve o próximo treino para o Coach IA analisar.'};const s=reportStats(cur);let previousVolume=0;if(mode==='week'){const all=Object.values(data.logs||{}).filter(validSavedLog);const end=new Date();end.setDate(end.getDate()-7);const start=new Date();start.setDate(start.getDate()-13);previousVolume=reportStats(all.filter(l=>{const d=new Date(l.date+'T12:00:00');return d>=start&&d<=end})).volume}if(mode==='month'){const d=new Date();d.setMonth(d.getMonth()-1);const p=d.toISOString().slice(0,7);previousVolume=reportStats(Object.values(data.logs||{}).filter(l=>validSavedLog(l)&&l.date.startsWith(p))).volume}const delta=previousVolume?((s.volume-previousVolume)/previousVolume)*100:null;const title=mode==='day'?'Análise do treino do dia':mode==='week'?'Análise semanal do desempenho':'Análise mensal do desempenho';const text=delta===null?'O período tem dados salvos, mas ainda não há período anterior suficiente para comparação.':`O volume ${delta>=0?'subiu':'caiu'} ${Math.abs(delta).toFixed(1)}% em relação ao período anterior.`;const next=s.avgRpe>=9?'Mantenha as cargas até confirmar boa recuperação.':s.volume>0?'Continue buscando o topo da faixa antes de aumentar a carga.':'Salve cargas e repetições para gerar uma análise melhor.';const score=Math.max(0,Math.min(10,8+(delta!==null?(delta>0?.8:-.8):0)-(s.avgRpe>=9?1:0))).toFixed(1);return{score,title,text,next,stats:s}}
function exportData(){const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='meu-treino-backup.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
function importData(input){const file=input?.files?.[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{try{const parsed=JSON.parse(reader.result);if(!parsed||typeof parsed!=='object'||!parsed.logs)throw new Error('Formato inválido');data=parsed;persist();render();alert('Backup importado com sucesso.')}catch(e){alert('Não foi possível importar este backup.')}};reader.readAsText(file)}
function dataPage(){return topNav()+`<section class="hero"><div class="eyebrow">Dados</div><h1>⚙️ Dados e conta</h1><p>Seus treinos ficam salvos neste aparelho e, quando conectado, podem ser sincronizados com a nuvem.</p></section><section class="card account"><div><span>Status</span><b>${user?.offline?'Offline • dados locais':'Conta conectada'}</b></div><div class="cloudbox ${cloudReady?'ok':''}">${cloudReady?'☁ Online • salvo':'☁ Local disponível'}</div></section><section class="card"><h2>💾 Backup</h2><p class="muted">Exporte uma cópia dos seus treinos ou importe um backup anterior.</p><div class="actions"><button class="primary" onclick="exportData()">⬇ Exportar backup</button><button class="secondary" onclick="document.getElementById('importDataInput').click()">⬆ Importar backup</button><input id="importDataInput" type="file" accept=".json,application/json" hidden onchange="importData(this)"></div></section>${user?.offline?'':'<section class="card"><div class="actions"><button class="danger" onclick="signOut()">Sair da conta</button></div></section>'}${bottomNav()}`}
function reportTable(ls){if(!ls.length)return '<div class="empty"><div class="emptyicon">📊</div><p>Nenhum treino salvo neste período.</p></div>';return '<div class="reportTable">'+ls.slice().reverse().map(l=>'<div class="reportTableRow"><span>'+esc(dateBR(l.date))+'</span><b>'+esc(l.day)+' • '+esc(workouts[l.day]?.title||'Treino')+'</b><span>'+Object.values(l.rows||{}).reduce((n,r)=>n+setsFrom(r).length,0)+' séries</span><strong>'+((volumeForLog(l)/1000).toLocaleString('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2}))+' t</strong></div>').join('')+'</div>'}
function setReportMode(mode){window.__reportMode=['day','week','month','all'].includes(mode)?mode:'week';current='Relatórios';render()}
function setReportExercise(name){reportExercise=String(name||'');render()}
function reports(){const mode=window.__reportMode||'week',ls=reportRange(mode),s=reportStats(ls),c=coachPeriod(mode);return topNav()+'<section class="hero compact"><div class="eyebrow">Coach IA • Relatórios</div><h1>Desempenho do treino</h1><p>Análise diária, semanal e mensal com volume e peso levantado.</p></section><div class="reportTabs"><button class="'+(mode==='day'?'active':'')+'" onclick="setReportMode(\'day\')">Hoje</button><button class="'+(mode==='week'?'active':'')+'" onclick="setReportMode(\'week\')">Semana</button><button class="'+(mode==='month'?'active':'')+'" onclick="setReportMode(\'month\')">Mês</button><button class="'+(mode==='all'?'active':'')+'" onclick="setReportMode(\'all\')">Tudo</button></div><div class="reportstats enhanced"><div class="stat"><span>Treinos</span><b>'+s.workouts+'</b></div><div class="stat"><span>Peso levantado</span><b>'+Math.round(s.volume).toLocaleString('pt-BR')+' kg</b></div><div class="stat"><span>Séries</span><b>'+s.sets+'</b></div><div class="stat"><span>RPE médio</span><b>'+((s.avgRpe!=null)?s.avgRpe.toFixed(1)+'/10':'—')+'</b></div></div><section class="card"><h2>🏋️ Peso levantado por treino</h2><p class="muted">Peso levantado = soma de carga × repetições de todas as séries.</p>'+reportTable(ls)+'</section><section class="card coachReportCard"><h2>🤖 Coach IA</h2><div class="coachScore"><b>'+c.score+'</b><div><strong>'+c.title+'</strong><small>Somente registros salvos</small></div></div><p>'+c.text+'</p><div class="coachNext"><b>🎯 Próximo passo:</b> '+c.next+'</div></section>'+bottomNav()}
function login(){return '<section class="card auth"><div class="authicon">🏋️</div><h1>Meu Treino</h1><p>Seu diário de musculação, cargas, repetições e progressão.</p><label>E-MAIL<input id="email" type="email" autocomplete="email" placeholder="seu@email.com"></label><label>SENHA<input id="password" type="password" autocomplete="current-password" placeholder="Sua senha"></label><button id="loginBtn" class="loginbtn" onclick="signIn()">ENTRAR</button><button class="secondary wide" onclick="signUp()">CRIAR CONTA</button></section>'}
function render(){const app=$('app');if(!app)return;if(!user){app.innerHTML=login();return}stopRestIntervals();const html=days.includes(current)?workout():current==='Histórico'?history():current==='Relatórios'?reports():dataPage();app.innerHTML=html+bottomNav();loadFilled();if(days.includes(current)){const k=key(current,activeDate());updateTimerDisplay(k);if(drafts[k]?.timerStartedAt)startGeneralTimer(k,drafts[k])}}
function stopRestIntervals(){Object.keys(restIntervals).forEach(i=>{clearInterval(restIntervals[i]);delete restIntervals[i]});Object.keys(restDebounce).forEach(i=>{clearTimeout(restDebounce[i]);delete restDebounce[i]})}
window.addEventListener('keydown',enterHandler,true);window.addEventListener('beforeunload',()=>{stopRestIntervals();if(generalTimer)clearInterval(generalTimer)});window.setVal=setVal;window.autoStartRep=autoStartRep;window.fieldInput=fieldInput;window.advanceField=advanceField;window.go=go;window.commitSaved=commitSaved;window.clearDay=clearDay;window.deleteLog=deleteLog;window.editLog=editLog;window.toggleRest=toggleRest;window.queueRestFromInput=queueRestFromInput;window.startGeneralTimer=startGeneralTimer;window.stopGeneralTimer=stopGeneralTimer;window.resetGeneralTimer=resetGeneralTimer;window.setReportMode=setReportMode;window.setReportExercise=setReportExercise;window.exportData=exportData;window.importData=importData;window.signIn=signIn;window.signUp=signUp;window.signOut=signOut;window.cleanLocalData=cleanLocalData;
(async()=>{await init()})();
