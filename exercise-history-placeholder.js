/* HISTÓRICO DE CARGA/REPS v4 — última informação real do exercício, em qualquer dia. */
(function(){
  const STORE='meu_treino_reset_v1';
  function read(){try{return JSON.parse(localStorage.getItem(STORE)||'{"logs":{}}')}catch(e){return {logs:{}}}}
  function today(){const d=new Date();return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')}
  function workoutList(day){try{if(typeof workouts!=='undefined'&&workouts[day])return workouts[day].ex||[];return window.workouts?.[day]?.ex||[]}catch(e){return []}}
  function exerciseIndex(day,name){return workoutList(day).findIndex(ex=>String(ex?.[0]||'').trim()===name)}
  function historyForExercise(name){const source=(typeof data!=='undefined'&&data?.logs)?data:read(),logs=Object.values(source.logs||{}),matches=[];for(const l of logs){if(!l||!l.day||!l.date||l.date===today())continue;const idx=exerciseIndex(l.day,name);if(idx<0)continue;const row=l.rows?.[idx];if(!row)continue;const sets=[];for(let s=0;s<10;s++){const kg=String(row['kg'+s]??'').trim(),reps=String(row['reps'+s]??'').trim();if(kg||reps)sets.push({kg,reps})}if(sets.length)matches.push({date:l.date,sets})}matches.sort((a,b)=>String(b.date).localeCompare(String(a.date)));return matches[0]?.sets||[]}
  function apply(){document.querySelectorAll('.exercise').forEach(card=>{const name=(card.querySelector('.exname')?.textContent||'').replace(/^\d+\.\s*/,'').trim();if(!name)return;const hist=historyForExercise(name);if(!hist.length)return;[...card.querySelectorAll('input.field')].forEach((input,i)=>{const set=Math.floor(i/2),kind=input.dataset.kind,value=hist[set]?.[kind==='kg'?'kg':'reps'];if(value&&!input.value){input.placeholder=value;input.classList.add('has-history-placeholder');input.title='Último registro: '+value}else if(input.value)input.classList.remove('has-history-placeholder')})})}
  if(!document.getElementById('history-placeholder-css')){const css=document.createElement('style');css.id='history-placeholder-css';css.textContent='input.field.has-history-placeholder::placeholder{color:#cbd5e1!important;opacity:.32!important;filter:blur(.45px)!important;font-weight:650}input.field.has-history-placeholder:focus::placeholder{opacity:.08!important}';document.head.appendChild(css)}
  document.addEventListener('input',e=>{if(e.target.matches('input.field'))e.target.classList.remove('has-history-placeholder')},true);
  const obs=new MutationObserver(()=>setTimeout(apply,30));obs.observe(document.body,{childList:true,subtree:true});setTimeout(apply,100);setTimeout(apply,800);setInterval(apply,1000);
})();
